package V5_Static;

public class Main {
  public static void main(String[] args) {
    /*
     * [static methods can]
     * 1. access static members
     * 2. access non-static members only
     * when they instantiated some non-static class
     *
     * [non-static methods can]
     * 1. access static members
     * 2. access non-static members of its own
     */
  }
}